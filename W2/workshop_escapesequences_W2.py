#the tab end sequence `\t` creates a tabbed space between objects
#different text files calculate the tab space differently based on links of strings and how they would place them

print(5.2, 52, 3, sep="\t")

#`\n` is a new line